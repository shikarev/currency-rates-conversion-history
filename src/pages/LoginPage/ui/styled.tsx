import { Box, styled } from '@mui/material'

export const LoginPageBoxStyled = styled(Box)({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  width: '100%',
  height: 'calc(100vh - 40px)',
})
